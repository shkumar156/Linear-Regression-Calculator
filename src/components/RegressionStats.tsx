import React from 'react';
import { RegressionResult } from '../types';

interface RegressionStatsProps {
  result: RegressionResult;
}

export function RegressionStats({ result }: RegressionStatsProps) {
  return (
    <div>
       
    </div>
  );
}